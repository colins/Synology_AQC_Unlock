v1.0.8
- Bug fix for v1.0.7 not excluding DS925+ (because it does not have a PCIe slot).
- Bug fix for v1.0.7 being set as beta.

v1.0.7
- Excluded DS925+ because it does not have a PCIe slot.

v1.0.6
- Now shows warning after starting if permissions aren't set.
- Open button now shows steps to set permissions if they aren't set.
- Added start, stop logging when permissions aren't set.
- Bug fix for not being able to start, repair or upgrade package if permissions aren't set.

v1.0.5
- Now shows warning after starting if permissions aren't set.
- Open button now shows steps to set permissions if they aren't set.
- Bug fix for not being able to start, repair or upgrade package if permissions aren't set.

v1.0.4 Beta
- Now shows warning after starting if permissions aren't set.
- Open button now shows steps to set permissions if they aren't set.

v1.0.3
- Bug fix for the installer not setting the required files as executable for the popup window (caused 502 error).

v1.0.2
- Added a DSM desktop notification that the AQC network card was loaded.
- Added window to set your preferred LAN port order for DSM to connect to other devices.
- Now hides Stop button in Package Center. If you stopped the package your network card would not load when you reboot the Synology.

v1.0.1
- First release.
